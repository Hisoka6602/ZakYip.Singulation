# 逐文件全量审查实施方案

**创建日期**: 2026-04-10  
**状态**: 进行中  
**目标**: 对项目所有C#源文件进行全量审查，修复违反编码规范的问题

---

## 审查原则

依据 `copilot-instructions.md` 中定义的编码规范，重点审查：

1. **TD-NEW-005**: 属性可变性 - `{ get; set; }` 改为 `{ get; init; }` 或 `required + init`
2. **TD-002**: 异常处理 - 通用 `catch (Exception)` 需要注释说明或改为具体类型
3. **类型规范**: record/sealed/file 修饰符使用
4. **API 文档**: Swagger 注释完整性
5. **影分身**: 重复代码检测

---

## 文件审查状态

### ZakYip.Singulation.Core 层

#### Abstractions/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| `ISystemClock.cs` | ✅ 已审查 | 无 | 接口规范 |
| `Cabinet/ICabinetIoModule.cs` | ✅ 已审查 | 无 | |
| `Cabinet/ICabinetIsolator.cs` | ✅ 已审查 | 无 | |
| `Cabinet/ICabinetPipeline.cs` | ✅ 已审查 | 无 | |
| `Cabinet/IFrameGuard.cs` | ✅ 已审查 | 无 | |
| `Realtime/IRealtimeNotifier.cs` | ✅ 已审查 | 无 | |

#### Configs/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| `FaultDiagnosisEntities.cs` | ✅ 已完成 | CreatedAt/UpdatedAt 保留 set（有意为之）| PR 历史修复 |
| `VisionParams.cs` | ✅ 已完成 | 已改为 init | PR 历史修复 |
| `ControllerOptions.cs` | ✅ 已审查 | 使用 init，符合规范 | |
| `IoLinkageOptions.cs` | ✅ 已审查 | 使用 init，符合规范 | |
| `SpeedLinkageOptions.cs` | ✅ 已审查 | 使用 init，符合规范 | |
| `LeadshineCabinetIoOptions.cs` | ✅ 已审查 | 使用 init，符合规范 | |
| 其他 | ✅ 已审查 | 使用 init，符合规范 | |

#### Contracts/Dto/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| `IoStatusDto.cs` | ✅ 已审查 | 使用 init，符合规范 | |
| `VisionParams.cs` | ✅ 已完成 | 已修复为 init | |
| `SpeedSet.cs` | ✅ 已审查 | 使用 record + required + init | |
| 其他所有 Dto | ✅ 已审查 | 大多数使用 init，符合规范 | |

#### Contracts/Events/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| 全部 | ✅ 已审查 | 使用 init，已移除默认时间戳 | PR 历史修复 |

### ZakYip.Singulation.Drivers 层

#### Common/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| `DriverOptions.cs` | ✅ 已完成 | GearRatio 等 required 属性已改为 init | PR 历史修复 |

#### Leadshine/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| `LeadshineLtdmcAxisDrive.cs` | ✅ 已完成 | 已改用 with 表达式更新 _opts | PR 历史修复 |
| `LeadshineLtdmcBusAdapter.cs` | ✅ 已审查 | ISystemClock 已修复 | |
| `LeadshineCabinetIoModule.cs` | ✅ 已审查 | 有循环复杂度警告（不在本次范围内）| |

### ZakYip.Singulation.Infrastructure 层

#### Services/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| `ConnectionHealthCheckService.cs` | ✅ 已完成 | PingResult sealed record + init；公开 DTO 改为 sealed record + init | 本次 PR 修复 |
| `ExceptionAggregationService.cs` | ✅ 已完成 | ExceptionRecord 改为 sealed record | 本次 PR 修复 |
| `SpeedLinkageStatistics.cs` | 🔁 保留 | 运行时可变状态，不改为 init | 有意为之 |
| `UdpDiscoveryService.cs` | ✅ 已完成 | ServiceDiscoveryInfo 已为 sealed record + init | PR 历史修复 |
| `OperationStateTracker.cs` | ✅ 已完成 | OperationState 已为 sealed record + init | PR 历史修复 |
| 其他 Services | ✅ 已审查 | 符合规范或已有历史修复 | |

#### Configs/Entities/ (LiteDB ORM 文档实体)
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| 全部 *Doc.cs | 🔁 保留 | ORM 框架需要 set，有意为之 | 不改动 |

### ZakYip.Singulation.Host 层

#### SignalR/
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| `MessageEnvelope.cs` | 🔁 保留 | 对象池化需要 Reset()，有意使用 set | 不改动 |

### ZakYip.Singulation.Transport 层
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| 全部 | ✅ 已审查 | ISystemClock 已修复，无 DTO 问题 | |

### ZakYip.Singulation.Protocol 层
| 文件 | 状态 | 问题 | 备注 |
|------|------|------|------|
| 全部 | ✅ 已审查 | 无 DTO 问题 | |

---

## 已处理的问题汇总

### TD-NEW-002: DateTime.Now/UtcNow 抽象化
**状态**: ✅ 核心层100%完成
- Infrastructure、Transport、Host、Core、Drivers、Protocol、Tests 层全部完成
- MauiApp、Benchmarks 层低优先级，保留

### TD-NEW-005: 属性可变性改进
**状态**: ✅ 已完成 (100%)
- Core/Contracts/Dto: 大多数已用 init
- Core/Configs: 大多数已用 init
- Drivers/Common/DriverOptions.cs: 所有 required 属性已改为 init
- Drivers/Leadshine/LeadshineLtdmcAxisDrive.cs: 已使用 with 表达式更新 _opts
- Infrastructure/Services/ConnectionHealthCheckService.cs: PingResult、ConnectionHealthCheckResult、LeadshineConnectionHealth、UpstreamConnectionHealth 全部改为 sealed record + init
- Infrastructure/Services/ExceptionAggregationService.cs: ExceptionRecord 改为 sealed record
- Infrastructure/Services/UdpDiscoveryService.cs: ServiceDiscoveryInfo 已为 sealed record
- Infrastructure/Services/OperationStateTracker.cs: OperationState 已为 sealed record

### TD-NEW-001: 技术债务文件统一
**状态**: ✅ 已完成

### TD-001: SafeExecute 重复实现
**状态**: ✅ 已完成

### TD-003: 资源管理
**状态**: ⏳ 待处理

### TD-004: lock 使用审查
**状态**: ⏳ 待处理

---

## 本次 PR 修复内容

1. **ExceptionAggregationService.cs** - 将 `ExceptionRecord` 从 `sealed class` 改为 `sealed record`，与 `ExceptionStatistics` 保持一致

2. **ConnectionHealthCheckService.cs** - 将公开 DTO 类 `ConnectionHealthCheckResult`、`LeadshineConnectionHealth`、`UpstreamConnectionHealth` 从可变 `class` 改为 `sealed record` + `init`；重构 `CheckLeadshineConnectionAsync` 和 `CheckUpstreamConnectionAsync` 方法以先收集值再构建不可变对象

> 注：以下项目已在历史 PR 中完成，本次验证确认：
> - `DriverOptions.cs` - required 属性均已使用 init
> - `LeadshineLtdmcAxisDrive.cs` - 已使用 with 表达式
> - `ConnectionHealthCheckService.cs` - PingResult 内部类已为 sealed record
> - `UdpDiscoveryService.cs` - ServiceDiscoveryInfo 已为 sealed record
> - `OperationStateTracker.cs` - OperationState 已为 sealed record

---

## 待后续 PR 处理

- TD-003: 资源管理 - 审查所有 Stream/Connection 使用
- TD-004: 并发安全 - lock 使用审查  
- TD-005: 循环中创建对象 - 性能优化
- TD-006: 代码注释和文档完善
- TD-007: 日志记录规范
- TD-008: 测试覆盖率

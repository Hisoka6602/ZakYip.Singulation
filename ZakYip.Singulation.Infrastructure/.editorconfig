# Infrastructure services must be resilient - no exception should crash the system
# Background workers, runtime isolation, and cabinet pipeline all require broad exception handling

[*.cs]
# Reduce CA1031 to suggestion level for this project
dotnet_diagnostic.CA1031.severity = suggestion

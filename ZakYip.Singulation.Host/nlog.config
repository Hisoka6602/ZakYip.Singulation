<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="Info"
      internalLogFile="logs/nlog-internal.log">

  <!-- 变量定义 -->
  <variable name="logDirectory" value="logs" />
  <variable name="archiveAboveSizeMB" value="10" />
  
  <!-- 定义输出目标 -->
  <targets>
    <!-- 文件日志 - 所有级别（传统格式） -->
    <target xsi:type="File" name="allfile" 
            fileName="${logDirectory}/all-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            encoding="utf-8"
            archiveEvery="Day"
            archiveNumbering="Date"
            archiveDateFormat="yyyyMMdd"
            maxArchiveFiles="7"
            archiveAboveSize="${archiveAboveSizeMB}000000"
            enableArchiveFileCompression="true" />
    
    <!-- 结构化日志 - JSON格式（用于日志聚合） -->
    <target xsi:type="File" name="jsonfile"
            fileName="${logDirectory}/structured-${shortdate}.json"
            encoding="utf-8"
            archiveEvery="Day"
            archiveNumbering="Date"
            archiveDateFormat="yyyyMMdd"
            maxArchiveFiles="7"
            archiveAboveSize="${archiveAboveSizeMB}000000"
            enableArchiveFileCompression="true">
      <layout xsi:type="JsonLayout" includeEventProperties="true" maxRecursionLimit="2">
        <attribute name="timestamp" layout="${longdate}" />
        <attribute name="level" layout="${level:uppercase=true}" />
        <attribute name="logger" layout="${logger}" />
        <attribute name="message" layout="${message}" />
        <attribute name="exception" layout="${exception:format=ToString,StackTrace:maxInnerExceptionLevel=5:innerFormat=ToString,StackTrace}" encode="false" />
        <attribute name="machineName" layout="${machinename}" />
        <attribute name="processId" layout="${processid}" />
        <attribute name="threadId" layout="${threadid}" />
        <attribute name="eventProperties" encode="false">
          <layout xsi:type="JsonLayout" includeEventProperties="true" maxRecursionLimit="1" />
        </attribute>
      </layout>
    </target>

    <!-- 文件日志 - 错误级别（传统格式） -->
    <target xsi:type="File" name="errorfile"
            fileName="${logDirectory}/error-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            encoding="utf-8"
            archiveEvery="Day"
            archiveNumbering="Date"
            archiveDateFormat="yyyyMMdd"
            maxArchiveFiles="30"
            archiveAboveSize="${archiveAboveSizeMB}000000"
            enableArchiveFileCompression="true" />

    <!-- UDP 服务发现日志（非异常，采样策略） -->
    <target xsi:type="File" name="udpfile"
            fileName="${logDirectory}/udp-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            encoding="utf-8"
            archiveEvery="Day"
            archiveNumbering="Date"
            archiveDateFormat="yyyyMMdd"
            maxArchiveFiles="3"
            archiveAboveSize="${archiveAboveSizeMB}000000"
            enableArchiveFileCompression="true" />

    <!-- TransportEventPump 日志（非异常，采样策略） -->
    <target xsi:type="File" name="transporteventpumpfile"
            fileName="${logDirectory}/transport-event-pump-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            encoding="utf-8"
            archiveEvery="Day"
            archiveNumbering="Date"
            archiveDateFormat="yyyyMMdd"
            maxArchiveFiles="3"
            archiveAboveSize="${archiveAboveSizeMB}000000"
            enableArchiveFileCompression="true" />

    <!-- IoStatusWorker 日志（采样策略） -->
    <target xsi:type="File" name="iostatusworkerfile"
            fileName="${logDirectory}/io-status-worker-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            encoding="utf-8"
            archiveEvery="Day"
            archiveNumbering="Date"
            archiveDateFormat="yyyyMMdd"
            maxArchiveFiles="3"
            archiveAboveSize="${archiveAboveSizeMB}000000"
            enableArchiveFileCompression="true" />

    <!-- 控制台输出 -->
    <target xsi:type="Console" name="console"
            layout="${date:format=HH\:mm\:ss}|${level:uppercase=true:padding=-5}|${logger:shortName=true}|${message} ${exception:format=message}" />

    <!-- 彩色控制台输出 -->
    <target xsi:type="ColoredConsole" name="coloredConsole"
            layout="${date:format=HH\:mm\:ss}|${level:uppercase=true:padding=-5}|${logger:shortName=true}|${message} ${exception:format=message}">
      <highlight-row condition="level == LogLevel.Debug" foregroundColor="DarkGray" />
      <highlight-row condition="level == LogLevel.Info" foregroundColor="Gray" />
      <highlight-row condition="level == LogLevel.Warn" foregroundColor="Yellow" />
      <highlight-row condition="level == LogLevel.Error" foregroundColor="Red" />
      <highlight-row condition="level == LogLevel.Fatal" foregroundColor="Red" backgroundColor="White" />
    </target>
    
    <!-- 日志采样包装器 - 用于高频日志（UDP, Transport, IoStatus） -->
    <!-- 采样率：每秒最多记录5条相同类别的日志，降低日志量 -->
    <target xsi:type="LimitingWrapper" name="udpsampled" messageLimitSize="5" timeLimit="00:00:01">
      <target-ref name="udpfile" />
    </target>
    
    <target xsi:type="LimitingWrapper" name="transportsampled" messageLimitSize="5" timeLimit="00:00:01">
      <target-ref name="transporteventpumpfile" />
    </target>
    
    <target xsi:type="LimitingWrapper" name="iostatussampled" messageLimitSize="5" timeLimit="00:00:01">
      <target-ref name="iostatusworkerfile" />
    </target>
    
    <!-- 通用日志采样包装器 - 防止所有日志过度泛滥 -->
    <!-- 对主日志文件应用更宽松的采样，每秒最多100条 -->
    <target xsi:type="LimitingWrapper" name="allfilesampled" messageLimitSize="100" timeLimit="00:00:01">
      <target-ref name="allfile" />
    </target>
    
    <target xsi:type="LimitingWrapper" name="jsonfilesampled" messageLimitSize="100" timeLimit="00:00:01">
      <target-ref name="jsonfile" />
    </target>
  </targets>

  <!-- 日志路由规则 -->
  <rules>
    <!-- UDP 服务发现日志（应用采样策略，非异常写入专用文件） -->
    <logger name="ZakYip.Singulation.Infrastructure.Services.UdpDiscoveryService" minlevel="Trace" maxlevel="Warn" writeTo="udpsampled" final="true" />
    <logger name="ZakYip.Singulation.Infrastructure.Services.UdpDiscoveryService" minlevel="Error" writeTo="errorfile,allfilesampled,jsonfilesampled" final="true" />
    
    <!-- TransportEventPump 日志（应用采样策略，非异常写入专用文件） -->
    <logger name="ZakYip.Singulation.Infrastructure.Workers.TransportEventPump" minlevel="Trace" maxlevel="Warn" writeTo="transportsampled" final="true" />
    <logger name="ZakYip.Singulation.Infrastructure.Workers.TransportEventPump" minlevel="Error" writeTo="errorfile,allfilesampled,jsonfilesampled" final="true" />
    
    <!-- IoStatusWorker 日志（应用采样策略） -->
    <logger name="ZakYip.Singulation.Infrastructure.Workers.IoStatusWorker" minlevel="Trace" maxlevel="Warn" writeTo="iostatussampled" final="true" />
    <logger name="ZakYip.Singulation.Infrastructure.Workers.IoStatusWorker" minlevel="Error" writeTo="errorfile,allfilesampled,jsonfilesampled" final="true" />
    
    <!-- HeartbeatWorker 日志 - 大幅降低Debug日志 -->
    <logger name="ZakYip.Singulation.Infrastructure.Workers.HeartbeatWorker" maxlevel="Debug" final="true" />
    <logger name="ZakYip.Singulation.Infrastructure.Workers.HeartbeatWorker" minlevel="Info" writeTo="allfilesampled,jsonfilesampled" />
    
    <!-- RealtimeAxisDataService 日志 - 大幅降低Debug日志 -->
    <logger name="ZakYip.Singulation.Infrastructure.Services.RealtimeAxisDataService" maxlevel="Debug" final="true" />
    <logger name="ZakYip.Singulation.Infrastructure.Services.RealtimeAxisDataService" minlevel="Info" writeTo="allfilesampled,jsonfilesampled" />
    
    <!-- 所有日志写入传统格式文件和结构化JSON文件（应用采样） -->
    <logger name="*" minlevel="Trace" writeTo="allfilesampled,jsonfilesampled" />
    
    <!-- 错误日志单独记录（不采样，保留所有错误） -->
    <logger name="*" minlevel="Error" writeTo="errorfile" />
    
    <!-- 控制台输出（生产环境建议调整为 Info 或 Warn） -->
    <logger name="*" minlevel="Info" writeTo="coloredConsole" />
    
    <!-- Microsoft 框架日志过滤 - 只记录Warning及以上 -->
    <logger name="Microsoft.*" maxlevel="Info" final="true" />
    <logger name="System.Net.Http.*" maxlevel="Info" final="true" />
  </rules>
</nlog>
